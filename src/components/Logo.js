const Logo = ({ height, width }) => (
  <div className="logo" style={{ height: height, width: width }}>
    <div className="logo-sq__big"></div>
    <div className="logo-sq__small"></div>
  </div>
);

export default Logo;
